# Zero Touch Provisioning (ZTP) Tool - Environment Configuration Template
#
# INSTRUCTIONS:
# 1. Copy this file to .env:  cp .env.template .env
# 2. Fill in all required values below
# 3. Keep .env file secure and never commit to version control
# 4. Add .env to .gitignore if not already present

# ==============================================================================
# NetBox Configuration
# ==============================================================================
# NetBox is the source of truth for device configurations and metadata

# NetBox server URL (include https://)
NETBOX_URL=https://netbox.example.com

# NetBox API authentication token
# Generate from NetBox: User Menu > API Tokens > Add Token
NETBOX_TOKEN=your_netbox_api_token_here

# Verify SSL certificates when connecting to NetBox (true/false)
# Set to 'false' only for development with self-signed certificates
VERIFY_SSL=true

# ==============================================================================
# Jump Host Configuration
# ==============================================================================
# Linux jump host that runs FTP service for configuration delivery

# Jump host IP address or hostname
JUMPHOST_IP=10.0.0.1

# SSH username for jump host
JUMPHOST_USERNAME=admin

# SSH password for jump host
# Alternative: Use SSH key authentication (configure in ssh_manager.py)
JUMPHOST_PASSWORD=your_jumphost_password

# FTP directory path on jump host (where config files are stored)
FTP_DIRECTORY=/srv/ftp

# ==============================================================================
# Terminal Server Configuration
# ==============================================================================
# Out-of-band terminal server for console access to network devices

# Terminal server IP address or hostname
TERMINAL_SERVER_IP=10.0.0.2

# SSH username for terminal server
TERMINAL_SERVER_USERNAME=admin

# SSH password for terminal server
TERMINAL_SERVER_PASSWORD=your_terminal_server_password

# ==============================================================================
# FTP Server Configuration
# ==============================================================================
# FTP server details for configuration file retrieval by network devices
# Note: This is typically the same as the jump host, but from the device's perspective

# FTP server IP address (accessible from network devices via management VRF)
# This should be the IP address that devices can reach
FTP_SERVER_IP=1.1.1.1

# FTP username for device authentication
FTP_USERNAME=ftpuser

# FTP password for device authentication
FTP_PASSWORD=your_ftp_password


# ==============================================================================
# Logging Configuration
# ==============================================================================

# Logging level: DEBUG, INFO, WARNING, ERROR
# Use DEBUG for troubleshooting, INFO for normal operation
LOG_LEVEL=INFO

# Log file path (relative or absolute)
# Logs directory will be created automatically if it doesn't exist
LOG_FILE=./logs/provisioning.log

# ==============================================================================
# Advanced Configuration (Optional)
# ==============================================================================

# SSH connection timeout in seconds
# SSH_TIMEOUT=30

# SSH connection retries
# SSH_RETRIES=3

# Console command timeout in seconds
# CONSOLE_TIMEOUT=120

# ==============================================================================
# SECURITY NOTES
# ==============================================================================
#
# 1. NEVER commit .env file to version control
# 2. Use strong passwords and rotate credentials regularly
# 3. Consider using SSH key authentication instead of passwords
# 4. Limit FTP user permissions to minimum required access
# 5. Use SSL/TLS for NetBox API (VERIFY_SSL=true in production)
# 6. Store API tokens securely and generate with minimal required permissions
# 7. Consider using a secrets management system (HashiCorp Vault, etc.)
#    for production deployments
#
# ==============================================================================
